/** Opciones para crear la imagen de volumen (indicador de sonido de la Wife) en la escena */
export interface VolumeDisplayConfig {
  x?: number;
  y?: number;
  width?: number;
  height?: number;
  depth?: number;
}
